## A note on canvases

The page is not a feed or a template. It is a small desktop surface where blocks can keep their relative composition across displays.

Markdown blocks fetch files from `public/content` at runtime, so prose can stay outside the canvas JSON.
