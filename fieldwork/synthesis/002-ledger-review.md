# review — reconstruction 002 ledger

Status: **review pass complete**  
Target: `fieldwork/synthesis/002-source-closed-reconstruction.md`

This review checks evidential fidelity and scope. It is not new evidence and is not independent of the model family that produced the reconstruction.

## Checks

1. **Boundedness** — supported at the stated scope. The finite four-state construction defeats the universal claim that boundedness alone entails incomplete coverage. The reconstruction correctly preserves the narrower capacity-relation repair as an added premise rather than a survived claim.

2. **Accessibility** — supported at the stated scope. The finite-set construction shows that arbitrary reachable subsets can be encoded post hoc as constraints or dynamics. The reconstruction correctly limits the result to vacuity of unrestricted descriptions and does not infer that real independently specified mechanisms are non-explanatory.

3. **Equivalence** — supported at the stated scope. Quotient-class identity can represent any equivalence relation, so unrestricted `preserves something` wording is non-discriminating. The independent-criterion repair remains quarantined.

4. **Structure taxonomy** — correctly classified as an anti-collapse taxonomy rather than a verified law. No promotion is implied by retaining it.

5. **Prediction / intervention / control** — correctly narrowed. The finite constructions establish separability in explicitly specified deterministic systems. They do not establish universal mutual independence. Causal-inference and control-theory distinctions provide compatible native controls, not proof of a Geometry theory.

6. **Learning / error** — scope correction is necessary and present. The Oja probe supports only that the tested implementation needs no external teacher target, reward, desired output, or separately supplied mismatch. It does not settle whether a broader error concept is explanatory or vacuous.

## Seed-link provenance spot checks

- Chern supports withdrawing a simple causal `invariants -> symmetries` story: Klein-style geometry specifies transformation groups and studies invariants, while later geometry is not reduced to that stage.
- Information bottleneck work supports demoting generic `compression -> prediction/control`: compression is relative to what information about a chosen target is preserved.
- Active-matter work supplies a concrete case of coherent large-scale order produced by local driven dynamics without requiring an invariant-searching learner, so `boundedness -> invariant search` is not licensed as a universal transition.

## Reviewer objections preserved

Two conclusions are allocation judgments rather than verified facts:

- `no unified general Geometry theory is currently entitled to active commitment`;
- `no new theoretical test is currently earned`.

They are defensible given the ledger, but should remain labeled as reconstruction decisions. They are not themselves probe results.

## Verdict

**Pass with scope corrections already incorporated.** No ledger row requires promotion. No failed clause is silently repaired. No new test contract is earned by this review.

The reconstruction can serve as the round-001 post-falsification integration artifact while the frozen seed, original backbone wording, and historical probe outcomes remain unchanged.