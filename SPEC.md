# Geometry Specification v1

This repo implements the v1 Geometry spec: a static React/Vite personal site made from
100vh canvases of draggable, resizable, grid-aligned blocks. Canvases live as JSON under
`src/content/canvases`, are bundled at build time, and are editable locally through the
Vite dev save plugin.
